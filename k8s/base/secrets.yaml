# Secrets template - actual values should be provided via:
# - Kustomize secretGenerator in overlays
# - External secret management (AWS Secrets Manager, Vault, etc.)
# - CI/CD pipeline
#
# This file uses placeholder values for structure reference only.
# DO NOT commit real secrets to version control!
apiVersion: v1
kind: Secret
metadata:
  name: coderev-secrets
  namespace: coderev
type: Opaque
stringData:
  # Database connection
  DATABASE_URL: "postgresql+asyncpg://user:password@host:5432/coderev"

  # Redis connection
  REDIS_URL: "redis://redis:6379/0"

  # GitHub token for API access
  GITHUB_TOKEN: "ghp_placeholder"

  # LLM API keys (optional - only needed if using that provider)
  ANTHROPIC_API_KEY: "sk-ant-placeholder"
  OPENAI_API_KEY: ""

  # GitHub webhook secret (optional)
  GITHUB_WEBHOOK_SECRET: ""